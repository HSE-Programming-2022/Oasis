<Page x:Class="Oasis.Design.AdminUsersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:Oasis.Design"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminUsersPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBox Name="SearchingTextBox" Grid.Column="0" Height="50" materialDesign:HintAssist.Hint="Search here...." Width="500" Foreground="White" Style="{StaticResource MaterialDesignFilledTextBox}" ></TextBox>
            <Button Grid.Column="1" Width="30" Height="30" Background="{x:Null}" HorizontalAlignment="Left" VerticalAlignment="Center" BorderBrush="{x:Null}"  Style="{StaticResource MaterialDesignIconButton}" materialDesign:ShadowAssist.ShadowDepth="Depth0" >
                <materialDesign:PackIcon Kind="Search"  Width="30" Height="30" Foreground="White"></materialDesign:PackIcon>
                 
            </Button>
        </Grid>
        <ListView Grid.Row="1" Width="700">
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="Login" Width="200" DisplayMemberBinding="{Binding Login}"/>
                        <GridViewColumn Header="Nickname" Width="150" DisplayMemberBinding="{Binding Nickname}"/>
                        <GridViewColumn Header="Balance" Width="130" DisplayMemberBinding="{Binding Ballance}"/>
                    </GridView.Columns>
                    
                </GridView>
            </ListView.View>
        </ListView>
        
        
        
    </Grid>
</Page>
